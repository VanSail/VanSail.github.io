// Ambient module declarations for custom Docusaurus theme components.
// These are resolved at build time by Docusaurus via the @theme alias,
// but tsc needs them declared for the standalone typecheck script.
declare module '@theme/Navbar/LogoCenter';
